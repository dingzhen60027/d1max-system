# 单楼层调试候选 — 2026-09-10

当前 Foxglove 左侧已选择下方 8 月 25 日单层候选，右侧实时双雷达/图像、底部状态/单向急停等原布局不变；不加载历史多层点云。没有启动新的建图、定位或导航。
`config/map-view.yaml` 为 `enabled: true`，仅包含 `floor1` 单层候选，显示其全部 497,844 个有效点。数据禁用时仍保留左侧窗口，不再移除布局。
本次没有终止现有监控父进程或 SDK。旧多层静态发布进程归父脚本的 `wait -n` 管理，直接终止会连带关闭整组监控，故仍仅从 Foxglove 退订/卸载。单层点云另用独立节点、点云话题和状态话题接入，不与旧发布器混写；未来正常启动读取本单层配置。
原 PCD 没有删除或修改，视图恢复备份为 `artifacts/layout-before-single-floor.json`。

## 优先核对的单层候选

`/home/dndx/d1max_nav_ws/maps/runs/20260825_235024/sc_pgo/optimized_map.pcd`

- Faster-LIO + SC-PGO 导出，497,844 点，7.60 MiB；XY 包围盒约 65.49 × 44.95 m。
- 98% 点云的 Z 范围为 -0.77～2.75 m，主要结构集中在一层高度。
- 对应 332 个优化轨迹位姿的 Z 范围约 -0.008～0.445 m，没有与跨楼层相当的轨迹高度变化。
- 项目交接文档 `/home/dndx/d1max_nav_ws/D1_MAX_PROJECT_HANDOVER.md` 第 492–520 行将该 PCD 与第一层路径 `floor1_perimeter_loop_z050.csv` 配套使用。
- 这支持将它作为单楼层调试候选，不证明覆盖了整层或已满足导航要求。全点 Z 范围仍为 -3.25～7.28 m，包含少量高低散点，并非严格分层裁剪结果。
- 回环日志共 10 条，均为 `reject_separation`；不能根据 optimized 文件名或旧交接文档的“闭环地图”称呼判断闭环成功。

同次前端原始 PCD：
`/home/dndx/d1max_nav_ws/maps/runs/20260825_235024/d1max_map_20260826_000522.pcd`
共 754,779 点，11.52 MiB，适合作为重新处理的原始输入，但也需清理散点。

## 其他发现

- `maps/pct_floor1/floor1_map_clean.ply`：553,066 点，Z=-1.0～3.6 m；是 PLY，不是原始 PCD，不能仅凭 floor1 名字假定与当前场景/上述地图同源。
- `maps/pct_20260825_235024/floor1_traversability_preview.ply`：64,019 点，Z=-1.0～0.09 m；是可通行性预览，不含完整建筑墙体，不应当作完整建图 PCD。
- 9 月 4 日 14:34 的 SC-PGO 文件是此次卸载的多层图，1,300,464 点、19.84 MiB；不作为单层默认地图。
- 部分早期 FAST-LIO2 输出存在百万米甚至更大的坐标异常，已排除；不能因为文件更小就推荐用于导航。

按用户后续要求已加载这一候选用于左侧显示；不裁剪、不覆盖原图，也没有执行机器狗动作。恢复窗口前、加载单层前的布局均保存在 `artifacts/` 及原生布局菜单。
