# 2D / 3D 工作台验证 · 2026-09-10

## 实现边界

使用真实 shadcn/ui Base 组件构建首页、全局侧栏、面包屑和对话框；保留 3D 点云预览、模块化参数、建图管理、结果对比与归档。Go2 的离线 PCD 投影、2D 笔画修整和版本管理迁移到独立 D1 数据目录，未启动或移植 Go2 的机器人控制进程。

“2D 导航”当前提供地图准备；定位、TF、Nav2 与 D1 Max 速度执行尚未接入，界面明确提示且禁用导航执行。没有伪造定位，也没有向机器人发运动或急停指令。

## 自动验证

- Python 单元/API 测试 19 项通过：包含现有 3D 归档删除/退出回收，新增 2D 投影坐标、尺寸上界、非法参数与损坏状态拒绝、源文件保留、滤波执行与 intensity 保留、新版本编辑、选用回退、归档删除保护、符号链接保护、重复任务互斥、取消与重启恢复、ZIP 导出及配置复现。
- 单 YAML 重放：两个连续修订版的累计笔画可重放到一致 PGM；输出目录已存在时拒绝覆盖。Go2 滤波方案也执行了真实流水线与重放测试。
- 浏览器隔离测试 `frontend/scripts/verify-portal.mjs`：从 3D 携带源 ID 到 2D；生成不自动选用；笔刷、撤销/重做、另存新版本；选用/回退；重命名和完整 ZIP；归档、选择删除与全部删除；Go2 滤波预设；原始 PCD SHA-256 不变；无机器人 API 写请求、无页面运行时错误。
- 1920、1440、1280、900、390 像素宽度检查：首页、3D、2D 版本和生成页面均无页面级横向溢出；移动侧栏点击后关闭。
- 生产只读测试 `frontend/scripts/verify-ui.mjs`：真实 PCD 正常预览，原 3D 参数模块、开关、排序控件和 Go2 YAML 方案可见；清理进程必须明确确认；所有页面路由正常。测试拦截全部非 GET/HEAD API，不可能执行生产写操作。
- 前端生产构建成功。Three.js 预览 chunk 仍有 >500 kB 的构建提示，但已按需加载，首页与 2D 不加载点云渲染代码。

浏览器测试报告及截图位于本机 `/tmp/d1max-portal-qa/`；生产首页截图 `/tmp/d1max-production-home.png`。这些是临时验收产物，不纳入源码仓库。

## 数据与部署核对

- 生产地址 `http://127.0.0.1:8766/#/`，由 `d1max-web-managed.service` 托管；没有改端口、通信中间件、Foxglove 布局或本机连接管理器。
- 更新前后真实数据数量保持：4 份原始点云、0 份处理结果、10 份规划产物、0 份归档、18 条异常记录。没有替用户生成/选用真实 2D 地图。
- 写入/删除测试仅使用 `/tmp/d1max-web-migration-geWng3/` 中 604 点的合成 PCD，测试生成的版本已经通过归档删除 API 清理；没有删除真实点云或 Go2 数据。
- `map.yaml` 使用 `free_thresh: 0.196`，保留灰色 205 为未知；不沿用 Go2 原 `.25` 阈值与未知编码不匹配的行为。

本次是离线地图功能与 UI 验收，不是实机可通行性、定位精度或导航成功率验收。
